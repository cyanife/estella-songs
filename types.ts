export type Song = {
  name: string
  artist: string
  category: string
  BVID: string
}

export type config = {
  title: string
  liveRoomId: string
  footer: string
}

export type FilterValue = {
  category: string
  keyword: string
}
